{
  "name": "yapi-to-ts-workspace",
  "private": true,
  "packageManager": "pnpm@10.17.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
